'use strict';

module.exports=function(grunt){
	//time how long task take. can help when iptimizing build times
	require('time-grunt')(grunt);
//automarcaly load reuired grunt tasks
reuire(jit-grunt)(grunt);
	//define the configuration for all tasks
	grunt.intiConfig({
		

	});


};
